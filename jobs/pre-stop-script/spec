name: pre-stop-script

templates:
  pre-stop.sh: bin/pre-stop

packages: []

properties:
  script:
    description: Script that should run before the VM stops, run as root user.
    example: |-
        #!/bin/bash
        echo "any husekeeping tasks that might fit the use case"
